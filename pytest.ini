[pytest]
markers =
    slow: long-running property/fuzz tests (deselect with -m "not slow")
addopts = --strict-markers
# img.py is the graphics library we were given (mentor-authored, OpenCV wrapper);
# it is excluded from our coverage gate because it needs a display to exercise.
