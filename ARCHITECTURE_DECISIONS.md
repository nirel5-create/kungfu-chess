# איך המערכת בנויה — ולמה

שמונה החלטות שקבעו את הצורה של הפרויקט, והנימוק לכל אחת.

---

## 1. המנוע דטרמיניסטי, והזמן מוזרק אליו

המנוע לא מסתכל על שעון. הוא יודע שעבר זמן רק כשמישהו מודיע לו:
‏`wait(30)` פירושו "עברו 30 מילישניות, תעדכן את הלוח".

ומכאן נובעת תכונה שנבדקת ב‑2000 משחקים בכל הרצה של שער הבדיקות:

```
wait(a) then wait(b)  ==  wait(a + b)
```

לא משנה איך חותכים את הזמן — אותם מהלכים באותם זמנים מובילים לאותו לוח.

**למה זה הבסיס לכל השאר.** שני דברים שונים לגמרי נשענים על התכונה הזו:

- **השרת שולט בזמן.** אילו המנוע קרא שעון בעצמו, עיכובי רשת היו משנים את
  תוצאת המשחק — ושני שחקנים היו רואים לוחות שונים.
- **שחזור אחרי קריסה אפשרי.** השרת החי מקדם בצעדים של 30ms כדי שהתנועה
  תיראה חלקה. השחזור מקדם בגושים גדולים, כי אף אחד לא מסתכל וכל המהלכים כבר
  ידועים. בלי התכונה הזו, השחזור היה מגיע ללוח **אחר**.

**איך זה מאומת.** התכונה נבדקת בכל הרצה של שער הבדיקות: `tools/fuzz_game.py`
מריץ 2000 משחקים אקראיים ומוודא בכל אחד שחלוקת הזמן אינה משנה את התוצאה.
השורה `time-slicing equivalence holds` בסוף ההרצה היא האישור.

**המקרה העדין ביותר** הוא כלי שקופץ בדיוק כשתוקף נוחת על התא שלו, כך שהקופץ
אוכל את המגיע. אותה רשימת מהלכים, מורצת בארבע חלוקות זמן שונות — משרת חי
בצעדים של 30ms ועד שחזור בשלוש קריאות בלבד — מגיעה לאותו לוח בדיוק. התוצאה
לא נשמרה בשום מקום; היא **חושבה מחדש** ויצאה זהה.‏

---

## 2. בשחזור אסור לדלג מעל אירוע

**מה שנשמר הוא רשימת המהלכים, לא הלוח.** לכל מהלך שני שדות: **מה** ו**מתי**
(זמן משחק מתחילת המשחק, לא שעון אמיתי).

```
(0 ms,    move (0,0) -> (0,3))
(2200 ms, jump (0,3))
```

השחזור לוקח את מצב הפתיחה, מריץ את הרשימה, ומגיע למצב שהיה רגע לפני הקריסה.

**והכלל שגילינו בטעות:** מותר לקפוץ בגוש אחד **בין** אירועים. **אסור** לקפוץ
**מעל** אירוע.

בניסיון הראשון השחזור התקדם 3500ms בגוש אחד ודילג מעל הקפיצה שבזמן 2200 —
**והתוצאה יצאה הפוכה**: התוקף שרד והקופץ נאכל.

**ומה שמסוכן בזה:** לא נזרקה שגיאה. הקוד רץ, הלוח היה חוקי, שום דבר לא קרס.
פשוט הכלי הלא נכון מת.

---

## 3. מצב מלא, לא אירועים — ולמה זה מתהפך בקנה מידה

השרת שולח בכל tick את **כל מצב הלוח**, לא "מה השתנה".

**למה זה נכון כאן.** כל הודעה היא האמת השלמה, ולכן שלושה דברים מגיעים בחינם:

- הודעה שאבדה לא משנה — הבאה מתקנת הכל
- קליינט שהתנתק וחזר מסתנכרן מיד, בלי לוגיקה מיוחדת
- צופה שנכנס באמצע רואה את הלוח כמו שהוא

עם אירועים, כל אחד מאלה דורש עבודה: מספור, זיהוי חורים, בקשת שידור חוזר,
ושליחת מצב התחלתי למצטרף.

**ואיפה זה מתהפך.** בשרת מקומי עם שני שחקנים התעבורה זניחה. ב‑10 מיליון
שחקנים:

```
full snapshots:  10,000,000 x 2,000 bytes x 10/sec = 1.6 Tbps
move commands:   5,000,000 x 54 bytes x 2          = 4.3 Gbps
```

**פי 374.** שם עוברים לאירועים.

> ההחלטה לא הייתה שגויה — קנה המידה שינה אותה. זו ההבחנה שמבדילה בין "לא
> ידעתי" לבין "ידעתי, בחרתי, ואני יודעת מתי הבחירה מתהפכת".

---

## 4. ממשק צר מאפשר החלפה

הפיצול לשרת וקליינט היה קטן להפתיע: המנוע, `Controller` ו‑`view/` לא
השתנו כלל.

**הסיבה:** `Controller` תלוי בשתי מתודות בלבד.

```python
self._engine.request_move(self._selection, cell)
self._engine.request_jump(cell)
```

זה כל מה שהוא יודע על המנוע. לכן במקום המנוע האמיתי הועבר לו
אובייקט שחושף את אותן שתיים ושולח אותן ברשת:

```python
class ClientProxy:
    def request_move(self, src, dst):
        self._send(protocol.move(src, dst))

    def request_jump(self, cell):
        self._send(protocol.jump(cell))
```

‏`Controller` לא ידע שהוא עבר לרשת. **הוא לא השתנה בשורה אחת.**

**אילו היה משתמש בעשר מתודות, היה צריך לשכפל עשר.** בגלל שהוא צריך
שתיים, הפרוקסי הוא שבע שורות.

**ואותו דפוס חזר שלוש פעמים נוספות:** `_SnapshotBoard` (שתי מתודות ל‑`Controller`‏
ול‑`BoardMapper`), `PanelOverlay` (שלוש ל‑`ScorePanel`), ו‑`GameRegistry(make_session)`.‏
בכל אחד מהם הקוד הקפוא נשאר קפוא.

---

## 5. ה‑bus: להוסיף בלי לשנות

**בלי bus**, לולאת הציור מכירה את כולם:

```python
while True:
    snapshot = link.snapshot()
    observer.observe(snapshot, ms)      # score
    sound.check(snapshot, prev)         # sound
    banner.check(snapshot, prev)        # animation
```

כל תוספת היא שורה חדשה בלולאה.

**עם bus**, הלולאה מפרסמת פעם אחת:

```python
bus.publish(topics.SNAPSHOT, snapshot)
```

ומי שרוצה להגיב נרשם במקום אחר. **הלולאה לא יודעת שהם קיימים.**

**וההוכחה יושבת בתיקיית ה‑assets:** `illegal_move.wav` קיים ולא מנוגן, כי
השרת מתעלם בשקט מפקודה לא חוקית. להוסיף אותו זה הודעת `error` מהשרת
ו‑`subscribe` אחד — **בלי לגעת בלולאה, בנגן, או בכל דבר שעובד.**

---

## 6. בעלות אינה חוק שחמט

השרת חוסם שחקן מלהזיז כלים של היריב. **החסימה לא נמצאת במנוע**, למרות
שהמנוע הוא זה שיודע מה חוקי.

**המנוע יודע מה חוקי במשחק. הוא לא יודע מי מותר לו לבקש.**

ויש לזה הוכחה קונקרטית: אילו המנוע היה בודק צבעים, **`app.py` היה נשבר** —
המשחק המקומי מבוסס על כך ששחקן אחד מזיז את שני הצדדים, וזה מצב נתמך.

| מי | השאלה שהוא עונה עליה |
|---|---|
| ‏`server.py` | מיהו השולח? (מהחיבור עצמו) |
| ‏`GameSession` | מותר לו להזיז את הכלי הזה? |
| ‏`GameEngine` | המהלך חוקי? |

**והצבע לא נשלח בהודעה.** אילו הקליינט היה מצהיר `"color": "b"`, מספיק היה
לשנות שורה אחת בקוד שלו כדי להזיז את כלי היריב. **השרת מזהה את השולח מהחיבור
עצמו, לא מהצהרה שלו.**

---

## 7. הקונטיינר אוכף גבול שהמכונה הסתירה

**מה שקרה:** השרת קרס בלולאה ולא עלה בכלל.

```
common/protocol.py: from view.observer import CaptureEntry
ModuleNotFoundError: No module named 'view'
```

**למה שום טסט לא תפס.** על מכונת הפיתוח כל התיקיות באותו מקום, אז הייבוא
עובד — גם כשהוא מפר כלל מרכזי: **`common/` הוא בסיס משותף ואינו יודע
שקיימת שכבת תצוגה.**

**בקונטיינר של השרת יש רק מה שהוא באמת צריך.** ‏`view/` לא נכנסת, כי היא
דורשת OpenCV שהשרת לא צריך. אז הייבוא קרס מיד.

**זה ההבדל:** קודם הכלל היה הסכמה בלבד, ואפשר היה להפר אותו בשקט. עכשיו‏
הוא בדיקה שרצה בכל בנייה.

---

## 8. כלי מודד את מה שהתבקש, ושותק על השאר

שלוש פעמים ביום אחד כלי החזיר ירוק ולא פעל כמצופה:

| הכלי | מה הוא לא אמר |
|---|---|
| ‏`coverage` | ש‑`server.py` — 1,231 שורות — בכלל לא נכלל במדידה |
| ‏`coverage` | ש‑`# pragma: no cover` בשורה נפרדת מתחת ל‑`def` **אינו מחריג כלום** |
| `.gitignore` | שהשורה נכתבה ב‑UTF‑16 ולכן git לא קורא אותה |

**בשלושתם הכלי רץ בהצלחה.** אף אחד מהם לא הזהיר.

וברגע אחד השער הכריז **"543 טסטים, 100% כיסוי"** בזמן שהשרת קרס בלולאה ולא
הצליח לעלות. שתי האמירות היו נכונות בו זמנית.

**המסקנה שהנחתה את כל הפרויקט:** לא להאמין להצהרה — לבדוק. כמעט כל באג
משמעותי בשלב השרת נתפס בבדיקה ידנית, לא בשער.

---

## מה נבנה ומה מתוכנן

הבחנה שחשוב שתהיה מפורשת:

**נבנה ורץ:** מנוע דטרמיניסטי · שרת WebSocket · חדרים · שידוך לפי דירוג ±100 ·
‏ELO · סיסמאות מוצפנות · countdown לניתוק · מסך בית · צליל · לוגים בשני
הצדדים · Docker Compose עם PostgreSQL.‏

**מתוכנן ומנומק ב‑`Server_Design.md`, לא ממומש:** פיצול לשערים · NATS · Redis ·
‏Game Allocator · אזורים גיאוגרפיים · event sourcing בקנה מידה.

המסמך `Server_Design.md` עונה על **איך המשחק היה נבנה ל‑100 מיליון משתמשים** —
ועבר design review.‏
